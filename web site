#include "crow.h"

int main() {
    crow::SimpleApp app;

    // Define a route for the homepage
    CROW_ROUTE(app, "/")([](){
        return "Welcome to Harivenu's website!";
    });

    // Start the server on port 8080
    app.port(8080).multithreaded().run();
}
